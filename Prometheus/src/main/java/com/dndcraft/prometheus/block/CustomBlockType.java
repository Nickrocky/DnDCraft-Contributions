package com.dndcraft.prometheus.block;

public enum CustomBlockType {
    STRAIGHT_RETEXTURE, //Removes the previous item completely and re-establishes it as a new item
    NOTEBLOCK, //Adds the block as a blockstate of noteblocks
    TRIPWIRE, //Adds the block as a blockstate of tripwires
    REDSTONE //Adds the block as a blockstate of redstone

}
