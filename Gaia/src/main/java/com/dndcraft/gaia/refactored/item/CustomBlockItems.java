package com.dndcraft.gaia.refactored.item;

/*import com.dndcraft.prometheus.api.PrometheusAPI;
import com.dndcraft.prometheus.api.block.CustomBuildingBlocks;
import lombok.Getter;
import org.bukkit.inventory.ItemStack;

@Getter
public enum CustomBlockItems {

    MUD_BLOCK(CustomBuildingBlocks.MUD_BLOCK),
    DIRT_PATHWAY(CustomBuildingBlocks.DIRT_PATHWAY),
    FAIR_DIRT_PATHWAY(CustomBuildingBlocks.FAIR_DIRT_PATHWAY),
    WHITESTONE(CustomBuildingBlocks.WHITESTONE),
    COBBLED_WHITESTONE(CustomBuildingBlocks.COBBLED_WHITESTONE),
    WHITESTONE_BRICKS(CustomBuildingBlocks.WHITESTONE_BRICKS);

    private final CustomBuildingBlocks customBuildingBlock;
    private final ItemStack itemStack;

    CustomBlockItems(CustomBuildingBlocks customBuildingBlock){
        this.customBuildingBlock = customBuildingBlock;
        this.itemStack = PrometheusAPI.getApi().getCustomBlockManager().getBuildingBlock(customBuildingBlock).toItemStack();
    }

}*/
